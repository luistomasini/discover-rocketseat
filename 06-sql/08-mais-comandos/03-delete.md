# DELETE

```sql
-- apaga todo o registro, toda a linha do que foi especificado através da PRIMARY_KEY
DELETE FROM aluno WHERE matricula = 4
-- dá pra fazer DELETE sem WHERE, mas não é recomendável, apaga tudo da tabela
```