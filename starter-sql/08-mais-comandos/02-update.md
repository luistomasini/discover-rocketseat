# UPDATE

```sql

-- altera algum dado que a gente tem na tabela
UPDATE aluno SET nome='Pedrinho Henrique', responsavel='Maria José' WHERE matricula = 2
-- se eu fizer sem o WHERE ele altera todos os campos especificados

```