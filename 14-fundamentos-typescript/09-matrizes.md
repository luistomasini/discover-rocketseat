# Matrizes

Existem duas maneiras de tipar arrays no TypeScript, confira os exemplos:

```typescript
let numbers: number[];
numbers = [1,2,3,4,5,6];

let users: Array<string>;
users = ['Rodrigo', 'João'];
```